server:
  port: 8091

familie:
  ef:
    mottak:
      uri: http://localhost:8092/api/
      brukernavn: brukerMottak
      passord: pwdMottak
tps:
  innsyn:
    uri: http://localhost:8091/familie/alene-med-barn/soknad-api/mockapi/
    brukernavn: userInnsyn
    passord: pwdInnsyn
s3:
  uri: http://s3.nais-rook.svc.nais.local/
  region: us-east-1
  brukernavn: user
  passord: pwd
  passordfrase: pass
  maxMbVedlegg: 20
funksjonsbrytere:
  enabled: false
  unleash:
    uri: http://localhost:4242/api
    environment: local
    applicationName: familie-ef-soknad-api

cors:
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"

logging:
  config: classpath:logback-spring-test.xml

no.nav.security.jwt:
  issuers: selvbetjening
  issuer.selvbetjening:
    discoveryurl: http://metadata
    accepted_audience: aud-localhost
    cookie_name: localhost-idtoken
    proxyurl:
